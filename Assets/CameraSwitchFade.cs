using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Cinemachine;

public class CameraSwitchFade : MonoBehaviour
{
    //[SerializeField] Camera cameraA;
    //[SerializeField] Camera cameraB;
    //[Range(0, 1f)]
    //[SerializeField] float percent = .5f;
    //[SerializeField] int steps = 10;
    //[SerializeField] float stepTime = 1;
    //[SerializeField] float waitTime = 5f;

    //private void Start()
    //{
    //    StartCoroutine(Cor());
    //}

    //IEnumerator Cor()
    //{
    //    percent = 0;
    //    yield return new WaitForSeconds(waitTime);
    //    for (int i = steps; i >= 0; i--)
    //    {
    //        percent = (float)i / (float)steps;
    //        SetCameras();
    //        yield return new WaitForSeconds(stepTime);
    //    }
    //}
    //void SetCameras()
    //{
    //    cameraA.rect = new Rect(0, 0, percent, 1);
    //    cameraB.rect = new Rect(percent, 0, 1 - percent, 1);
    //} 
}